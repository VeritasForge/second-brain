---
created: 2026-02-03
source: claude-code
tags:
  - claude-code
  - deep-research
  - skills
  - invocation-rate
  - optimization
  - hooks
---

# Deep Research: Claude Code Skills 호출 빈도 향상 전략

## Executive Summary

Claude Code Skills의 자동 호출률(auto-invocation rate)은 기본 상태에서 약 **20%**에 불과합니다. 이는 Skills가 **순수 LLM 추론 기반 라우팅**을 사용하기 때문이며, 알고리즘적 매칭이 아닌 Claude의 자연어 이해에 의존합니다. 그러나 **description 최적화(→50%)**, **CLAUDE.md 참조(→60-70%)**, **Forced Eval Hook(→84%)**, **예제 추가(→90%)**를 통해 호출률을 극적으로 개선할 수 있습니다.

> 비유: Skills의 자동 호출은 도서관에서 사서(Claude)에게 "필요한 책을 알아서 가져다 주세요"라고 하는 것과 같습니다. 책등(description)에 적힌 제목이 모호하면 사서가 그 책을 찾지 못합니다. 제목을 명확히 하고(description 최적화), "이런 상황에서는 이 책을 가져오세요"라고 메모(Hook)를 남기면 성공률이 급격히 올라갑니다.

---

## 조사 흐름

```
┌──────────────────────────────────────────────────────────────┐
│                    Research Flow                              │
│                                                               │
│  WebSearch ×6 (병렬)                                         │
│  ├── 호출률 20% 문제 검색                                    │
│  ├── auto-invocation 최적화 방법                             │
│  ├── Boris Cherny skills 관련 팁                             │
│  ├── description 최적화 공식 가이드                          │
│  ├── Reddit SNS 검색                                         │
│  └── 정규봉 skills 최적화 검색                               │
│           │                                                   │
│  WebFetch ×6 (심화)                                          │
│  ├── scottspence.com (호출률 테스트 결과)        ← 핵심 자료 │
│  ├── scottspence.com (자동 활성화 워크어라운드)               │
│  ├── paddo.dev (Hook 기반 솔루션)                            │
│  ├── gist/mellanon (활성화 통계 데이터)          ← 핵심 자료 │
│  ├── platform.claude.com (공식 베스트 프랙티스)  ← 핵심 자료 │
│  └── blog.fsck.com (character budget 문제)                   │
└──────────────────────────────────────────────────────────────┘
```

---

## 1. 왜 Skills 호출률이 20%인가?

### 근본 원인 `[Confirmed]`

Skills의 라우팅은 **순수 LLM 추론(pure LLM reasoning)**에 의존합니다. 키워드 매칭이나 알고리즘 기반 intent detection이 아닙니다.

```
사용자 프롬프트
    │
    ▼
┌─────────────────────────────────┐
│ Claude의 시스템 프롬프트         │
│                                  │
│ <available_skills>               │
│   skill-1: "description..."     │  ← description만 존재
│   skill-2: "description..."     │     (전체 SKILL.md 아님)
│   skill-3: "description..."     │
│ </available_skills>              │
│                                  │
│ + 대화 히스토리                  │
│ + CLAUDE.md                      │
│ + Rules                          │
│ + 기타 컨텍스트                  │
└─────────────────────────────────┘
    │
    ▼
Claude가 "이 프롬프트에 맞는 스킬이 있나?" 판단
    │
    ├── 매칭 판단 ──→ Skill(skill-name) 호출
    │
    └── 매칭 안됨 ──→ 직접 처리 (80% 확률)
```

### 구체적인 실패 원인들

| 원인 | 설명 | 확신도 |
|------|------|--------|
| **모호한 description** | "Helps with documents" 같은 설명은 Claude가 매칭하지 못함 | `[Confirmed]` |
| **15,000자 예산 초과** | 스킬이 많으면 일부가 시스템 프롬프트에서 잘림 → Claude가 인지조차 못함 | `[Confirmed]` |
| **수동적 제안 무시** | "If the prompt matches any available skill keywords, use Skill()" 같은 지시는 배경 소음으로 무시됨 | `[Confirmed]` |
| **컨텍스트 경쟁** | description이 대화 히스토리, CLAUDE.md 등과 토큰을 경쟁 | `[Confirmed]` |
| **LLM의 본질적 한계** | Claude는 도구를 사용하지 않고 직접 해결하려는 경향이 있음 | `[Likely]` |

---

## 2. 호출률 향상 전략 (단계별)

### 전체 성공률 비교 `[Confirmed]`

```
호출 성공률 (%)

0%    20%       50%       70%   80%  84%  90%  100%
├──────┼──────────┼──────────┼─────┼────┼────┼────┤
│█████ │          │          │     │    │    │    │
│ 20%  │          │          │     │    │    │    │  기본 (단순 지시)
│      │██████████│          │     │    │    │    │
│      │   50%    │          │     │    │    │    │  Description 최적화
│      │          │██████████│     │    │    │    │
│      │          │ 60-70%   │     │    │    │    │  CLAUDE.md 참조
│      │          │          │█████│    │    │    │
│      │          │          │ 72% │    │    │    │  예제 포함
│      │          │          │     │████│    │    │
│      │          │          │     │80% │    │    │  LLM Eval Hook
│      │          │          │     │    │████│    │
│      │          │          │     │    │84% │    │  Forced Eval Hook
│      │          │          │     │    │    │████│
│      │          │          │     │    │    │90% │  예제 + 강제 평가
└──────┴──────────┴──────────┴─────┴────┴────┴────┘
```

> 출처: Scott Spence의 200+ 프롬프트 테스트 결과

---

### Level 1: Description 최적화 (20% → 50%) `[Confirmed]`

**가장 낮은 노력으로 가장 큰 효과**를 얻는 방법입니다.

#### 핵심 원칙: WHEN + WHEN NOT 패턴

```yaml
# BAD (20% 호출률)
---
name: api-conventions
description: Helps with API design
---

# GOOD (50% 호출률)
---
name: api-conventions
description: REST API design conventions for this codebase.
  Enforces kebab-case URLs, camelCase JSON, pagination for lists.
  Use when creating API endpoints, reviewing API code, or when
  user mentions REST, endpoint, or API design.
  Do NOT load for general backend discussions unrelated to API endpoints.
---
```

#### 공식 권장 description 작성법 `[Confirmed]`

| 원칙 | 설명 | 예시 |
|------|------|------|
| **3인칭으로 작성** | 시스템 프롬프트에 삽입되므로 관점 일관성 필요 | "Processes Excel files" O / "I can help you" X |
| **"Use when..." 포함** | 명시적 트리거 조건 기술 | "Use when working with PDF files or when user mentions PDFs" |
| **구체적 키워드** | 파일 확장자, 도구명, 동작 포함 | ".xlsx files, spreadsheets, tabular data, pivot tables" |
| **WHEN NOT 경계** | 불필요한 호출 방지 | "Do NOT load for general data discussions" |
| **1024자 이내** | 공식 최대 길이 | 간결하되 충분한 트리거 포함 |

#### 네이밍 컨벤션 `[Confirmed]`

Anthropic은 **동명사(gerund) 형태**를 공식 권장합니다:

```
GOOD: processing-pdfs, analyzing-spreadsheets, managing-databases
OK:   pdf-processing, process-pdfs
BAD:  helper, utils, tools, documents
```

---

### Level 2: CLAUDE.md에 스킬 참조 추가 (50% → 60-70%) `[Likely]`

```markdown
# CLAUDE.md

## Available Skills
- API 엔드포인트 작업 시 → /api-conventions 스킬 활용
- 보안 코드 리뷰 시 → /security-review 스킬 활용
- PR 생성 시 → /create-pr 스킬 활용
```

이 방법은 Claude에게 "이런 스킬들이 있다"는 것을 한 번 더 상기시켜 줍니다.

---

### Level 3: Hook 기반 강제 활성화 (→ 80-84%) `[Confirmed]`

Skills의 근본적 한계("Claude가 알아서 판단")를 우회하는 방법입니다.

#### 방법 A: Forced Eval Hook (84% 성공률) `[Confirmed]`

**원리**: Claude가 구현을 시작하기 **전에** 모든 스킬을 평가하고 YES/NO를 명시적으로 작성하도록 강제합니다.

```
사용자 프롬프트 → Hook 실행 → "INSTRUCTION: 각 스킬에 대해 YES/NO 평가를 작성하라" → Claude 평가 → 매칭 스킬 활성화 → 구현
```

**구현 예시** (`settings.json`):

```json
{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '🔍 INSTRUCTION: Before implementing, evaluate EVERY available skill. For each skill, write YES or NO with a one-line reason. Then activate matching skills with Skill(name) before proceeding.'"
          }
        ]
      }
    ]
  }
}
```

**장단점**:

| 장점 | 단점 |
|------|------|
| 84% 성공률 (가장 높음) | 장황한 출력 (토큰 소비 증가) |
| 외부 API 불필요 | 매 프롬프트마다 평가 오버헤드 |
| 일관적 성능 | 단순 질문에도 평가 실행 |

#### 방법 B: LLM Eval Hook (80% 성공률) `[Confirmed]`

**원리**: Claude Code 실행 **전에** 별도 API 호출로 스킬 매칭을 사전 평가합니다.

```
사용자 프롬프트 → 외부 API(Haiku)로 스킬 매칭 → 매칭 결과를 INSTRUCTION으로 주입 → Claude 실행
```

| 속성 | Forced Eval | LLM Eval |
|------|:-----------:|:--------:|
| 성공률 | **84%** | 80% |
| 비용/프롬프트 | $0.0067 | **$0.0061** |
| 일관성 | **높음** | 가변적 |
| 외부 의존성 | **없음** | API 필요 |
| 속도 | 느림 | **17% 빠름** |
| 최악의 경우 | 약간 저하 | **완전 실패 가능** (특정 시나리오 0/10) |

#### 방법 C: 파일 경로 기반 Hook (결정론적) `[Confirmed]`

**원리**: 작업 중인 파일 경로를 감지하여 관련 스킬을 자동 로드합니다.

```json
// skill-rules.json
{
  "rules": [
    {
      "paths": ["src/api/**/*.ts"],
      "skills": ["api-conventions", "error-handling"]
    },
    {
      "paths": ["tests/**"],
      "skills": ["testing-patterns"]
    }
  ]
}
```

**장점**: 예측 가능, 결정론적, semantic matching 불확실성 제거
**단점**: 파일이 없는 상황(계획 단계)에서 미작동, 의도하지 않은 토큰 소비

---

### Level 4: 예제 추가 (→ 90%) `[Confirmed]`

Description에 예제 시나리오를 포함하면 호출률이 72%에서 90%까지 향상됩니다.

```yaml
---
name: security-review
description: >
  Security code review specialist. Checks for OWASP Top 10 vulnerabilities,
  authentication flaws, and secrets exposure.
  Use when: reviewing code for security, checking auth logic,
  or when user mentions "security", "vulnerability", "OWASP", "injection".
  Example triggers:
  - "review this code for security issues"
  - "check if there are any vulnerabilities"
  - "is this auth implementation secure?"
  Do NOT use for: general code review, performance optimization.
---
```

---

### Level 5: 토큰 예산 확보 `[Confirmed]`

스킬이 많은 경우 15,000자 기본 예산을 초과하여 일부 스킬이 **아예 Claude에게 보이지 않을** 수 있습니다.

```bash
# 환경변수로 예산 확장
SLASH_COMMAND_TOOL_CHAR_BUDGET=30000 claude

# 또는 셸 설정에 추가
export SLASH_COMMAND_TOOL_CHAR_BUDGET=30000
```

**진단 방법**: `/context` 명령으로 제외된 스킬이 있는지 확인 `[Confirmed]`

---

## 3. 전략별 적합도 가이드

```
┌─────────────────────────────────────────────────────────┐
│              어떤 전략을 선택할까?                       │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Q1: 스킬이 5개 미만인가?                               │
│  ├── YES → Description 최적화만으로 충분                │
│  │          (WHEN + WHEN NOT 패턴)                      │
│  │                                                      │
│  └── NO → Q2: 스킬이 15개 이상인가?                    │
│      ├── YES → 토큰 예산 확장 필수                      │
│      │          + Description 간결화                    │
│      │                                                  │
│      └── NO → Q3: 호출 실패가 치명적인가?              │
│          ├── YES → Forced Eval Hook (84%)              │
│          │          또는 직접 /skill-name 사용          │
│          │                                              │
│          └── NO → Description 최적화 + CLAUDE.md 참조  │
│                    (60-70%면 충분)                      │
└─────────────────────────────────────────────────────────┘
```

---

## 4. Boris Cherny의 관련 인사이트

Boris Cherny는 Skills 호출률에 대해 직접적으로 다루지는 않았지만, 관련된 핵심 원칙을 제시했습니다:

| 원칙 | Skills 호출률과의 관계 | 확신도 |
|------|------------------------|--------|
| **"하루에 한 번 이상 하는 건 스킬로"** | 자주 쓰는 워크플로우를 스킬로 만들되, `/skill-name`으로 명시적 호출 권장 | `[Confirmed]` |
| **"slash command는 inner loop에"** | 자동 호출에 의존하지 말고, 반복 작업은 `/command`로 명시 호출 | `[Confirmed]` |
| **"CLAUDE.md 2.5k 토큰"** | 팀의 CLAUDE.md는 간결하게 → 스킬 description에 더 많은 예산 확보 | `[Confirmed]` |
| **검증 루프** | 스킬이 올바르게 호출되었는지도 검증 대상 | `[Likely]` |

> Boris의 접근: 자동 호출에 과도하게 의존하지 않고, **명시적 호출(`/skill-name`)을 기본으로** 사용. 자동 호출은 "보너스"로 취급.

---

## 5. 공식 문서의 핵심 권장사항 `[Confirmed]`

Anthropic의 [Skill authoring best practices](https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices)에서:

### Description 작성 체크리스트

- [ ] 3인칭으로 작성 ("Processes..." not "I can help...")
- [ ] "Use when..." 언어 포함
- [ ] 구체적 키워드/파일 확장자 포함
- [ ] 1024자 이내
- [ ] WHEN NOT 경계 명시

### 평가 기반 개발 (Evaluation-Driven Development)

공식 문서가 강력히 권장하는 접근:

```
1. 스킬 없이 작업 수행 → 실패 지점 기록
2. 평가 시나리오 3개 생성
3. 기준선 측정 (스킬 없이)
4. 최소한의 내용으로 스킬 작성
5. 평가 실행 → 비교 → 반복
```

### "Claude A/Claude B" 패턴

- **Claude A**: 스킬을 설계하고 개선하는 "전문가" 인스턴스
- **Claude B**: 실제 작업에서 스킬을 사용하는 "사용자" 인스턴스
- Claude B의 행동을 관찰 → Claude A에게 피드백 → 반복

---

## 6. Edge Cases & Caveats

| 상황 | 영향 | 권고 |
|------|------|------|
| Forced Eval Hook + 단순 질문 | 불필요한 평가 오버헤드 | 조건부 실행 (특정 키워드 감지 시만) |
| LLM Eval Hook + Form/Route 패턴 | 완전 실패 (0/10) | 특정 도메인에서는 Forced Eval로 |
| 15,000자 예산 초과 | 스킬이 아예 보이지 않음 | `/context`로 진단, 예산 확장 |
| description에 XML 태그 | 유효성 검증 실패 | 순수 텍스트만 사용 |
| Background 서브에이전트에서 Skill | Skill 도구 사용 불가 | Foreground에서 실행 |
| 파일 경로 Hook + 계획 단계 | Hook 미작동 (파일 없음) | semantic 매칭과 병행 |

---

## 7. 실전 적용 권장 조합

대부분의 팀에 가장 효과적인 **실용적 조합**:

```yaml
# 1단계: Description 최적화 (기본)
# 모든 스킬에 WHEN + WHEN NOT 패턴 적용

# 2단계: CLAUDE.md에 스킬 참조 추가
# "API 작업 시 /api-conventions 활용"

# 3단계: 핵심 스킬은 disable-model-invocation: true
# 중요한 워크플로우는 명시적 호출로 100% 보장

# 4단계: (선택) Forced Eval Hook
# 자동 호출이 중요한 팀 환경에서만
```

**현실적 권장**: 자동 호출률 100%를 목표로 하지 마세요. Boris Cherny처럼 **핵심 워크플로우는 `/skill-name`으로 명시 호출**하고, 자동 호출은 편의 기능으로 활용하는 것이 가장 안정적입니다.

---

## Contradictions Found

| 모순 | 출처 A | 출처 B | 해결 |
|------|--------|--------|------|
| "Skills는 자율적으로 활성화" vs "20% 성공률" | 공식 문서 | 커뮤니티 테스트 | 공식 문서는 메커니즘을 설명, 실제 성공률은 description 품질에 크게 의존. 양쪽 모두 맞지만, 관점이 다름 |
| Forced Eval이 "최선" vs Boris의 "명시 호출" | Scott Spence | Boris Cherny | 두 접근 모두 유효. Boris는 단순함을 선호(명시 호출), Scott은 자동화를 선호(Hook). 팀 규모와 워크플로우에 따라 선택 |

---

## Sources

1. [How to Make Claude Code Skills Activate Reliably](https://scottspence.com/posts/how-to-make-claude-code-skills-activate-reliably) — 기술 블로그 (200+ 테스트 데이터)
2. [Claude Code Skills Don't Auto-Activate (a workaround)](https://scottspence.com/posts/claude-code-skills-dont-auto-activate) — 기술 블로그
3. [Skills Auto-Activation via Hooks](https://paddo.dev/blog/claude-skills-hooks-solution/) — 기술 블로그
4. [Claude Code Skills Structure and Usage Guide](https://gist.github.com/mellanon/50816550ecb5f3b239aa77eef7b8ed8d) — 커뮤니티 (활성화 통계)
5. [Skill authoring best practices - 공식](https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices) — 공식 문서
6. [Extend Claude with skills - 공식](https://code.claude.com/docs/en/skills) — 공식 문서
7. [Claude Code Skills Not Triggering](https://blog.fsck.com/2025/12/17/claude-code-skills-not-triggering/) — 기술 블로그
8. [Boris Cherny Claude Code Tips](https://paddo.dev/blog/how-boris-uses-claude-code/) — 1차 자료
9. [10 Tips from Inside the Claude Code Team](https://paddo.dev/blog/claude-code-team-tips/) — 기술 블로그
10. [Claude Agent Skills: A First Principles Deep Dive](https://leehanchung.github.io/blogs/2025/10/26/claude-skills-deep-dive/) — 기술 블로그

## Research Metadata

- 검색 쿼리 수: 6 (일반 5 + SNS 1)
- 수집 출처 수: 10
- 출처 유형 분포: 공식 2, 1차 1, 블로그 6, 커뮤니티 1
- 확신도 분포: Confirmed 22, Likely 3, Uncertain 0, Unverified 0
- WebFetch 상세 조회: 6회
- SNS 출처: Reddit 직접 결과 없음
- 정규봉 관련: Skills 호출 빈도에 대한 직접적 자료는 미발견
