---
created: 2026-02-10
source: claude-code
tags:
  - backport
  - git
  - cherry-pick
  - release-management
---

# 📖 Backport — Concept Deep Dive

> 💡 **한줄 요약**: Backport는 최신 브랜치(main/develop)에 적용된 변경사항(버그 수정, 보안 패치 등)을 이전 릴리즈 브랜치에 선택적으로 가져오는 소프트웨어 릴리즈 관리 기법이다.

---

## 1️⃣ 무엇인가? (What is it?)

**Backport**(백포트)는 소프트웨어의 **새로운 버전에 적용된 변경사항을 이전(오래된) 버전에 이식**하는 행위를 말한다. 주로 보안 패치, 버그 수정, 핵심 기능 업데이트를 아직 운영 중인 이전 릴리즈에 전파할 때 사용된다.

- **탄생 배경**: 소프트웨어가 여러 버전을 동시에 유지보수(LTS 등)하는 관행이 보편화되면서 자연스럽게 발전. Linux Kernel, Debian 등 대규모 오픈소스 프로젝트에서 오랜 역사를 가짐
- **핵심 문제 해결**: "main 브랜치에서 고친 보안 취약점을 아직 운영 중인 v2.1.x 브랜치에도 적용해야 한다" 와 같은 **다중 릴리즈 유지보수** 문제
- **Git에서의 구현**: 주로 `git cherry-pick` 명령으로 특정 커밋을 대상 브랜치에 적용

> 📌 **핵심 키워드**: `cherry-pick`, `release branch`, `hotfix`, `LTS`, `maintenance branch`

---

## 2️⃣ 핵심 개념 (Core Concepts)

```
┌─────────────────────────────────────────────────────────────┐
│                    Backport 핵심 구조                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  main/develop ─────●────●────●────●────●────●───►          │
│                    │              │                          │
│              branch cut      fix commit (A)                 │
│                    │              │                          │
│                    ▼              │ cherry-pick              │
│  release/v2.1 ─────●────●────●───●'──►                     │
│                                  ▲                          │
│                            backported (A')                  │
│                                                             │
│  release/v2.0 ─────●────●────●───●''──►                    │
│                                  ▲                          │
│                            backported (A'')                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

| 구성 요소         | 역할       | 설명                                                  |
| ----------------- | ---------- | ----------------------------------------------------- |
| **Source Branch** | 원본       | 변경사항이 최초로 머지된 브랜치 (main/develop)         |
| **Target Branch** | 대상       | 백포트를 받을 릴리즈 브랜치 (release/v2.x)             |
| **Cherry-pick**   | 이식 도구  | 특정 커밋을 선택하여 다른 브랜치에 적용하는 Git 명령    |
| **Backport PR**   | 전달 단위  | 백포트된 변경사항을 담은 Pull Request                  |
| **Conflict Resolution** | 충돌 해결 | 코드 컨텍스트 차이로 인한 충돌 수동 해결          |

- **단방향 흐름**: 항상 "새로운 → 오래된" 방향으로 변경사항이 이동
- **Main-first 원칙**: 모든 수정은 반드시 main에 먼저 머지한 후 백포트 ([Bareos Git Workflow](https://docs.bareos.org/DeveloperGuide/gitworkflow.html))
- **커밋 단위 이식**: 전체 브랜치를 머지하는 것이 아니라, 필요한 커밋만 선별

---

## 3️⃣ 아키텍처와 동작 원리 (Architecture & How it Works)

```
┌─────────────────────────────────────────────────────────────┐
│                  Backport Workflow Architecture              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   Developer          Git Repo           CI/CD Pipeline      │
│   ┌──────┐          ┌──────────┐       ┌──────────────┐    │
│   │ Fix  │──merge──►│  main    │       │  Automated   │    │
│   │ Bug  │          │  branch  │       │  Backport    │    │
│   └──────┘          └────┬─────┘       │  Bot/Action  │    │
│                          │              └──────┬───────┘    │
│                          │ label:              │            │
│                          │ backport/v2.1       │            │
│                          │                     │            │
│                  ┌───────▼─────────┐           │            │
│                  │  PR merged to   │───trigger──┘            │
│                  │  main + label   │                         │
│                  └───────┬─────────┘                         │
│                          │                                   │
│              ┌───────────▼───────────┐                      │
│              │  cherry-pick to       │                      │
│              │  release/v2.1         │                      │
│              └───────────┬───────────┘                      │
│                          │                                   │
│              ┌───────────▼───────────┐                      │
│              │  Clean?               │                      │
│              │  ┌─Yes──► Auto PR     │                      │
│              │  └─No───► Manual fix  │                      │
│              └───────────────────────┘                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 🔄 동작 흐름 (Step by Step)

1. **Fix on Main**: 버그/보안 이슈를 main 브랜치에서 수정하고 PR을 생성하여 머지
2. **Label 부착**: 백포트가 필요한 PR에 `backport/v2.1` 같은 레이블을 추가
3. **Cherry-pick 실행**: 해당 커밋을 대상 릴리즈 브랜치에 cherry-pick

   ```bash
   git checkout release/v2.1
   git cherry-pick -x <commit-sha>
   # -x 플래그: 원본 커밋 해시를 메시지에 기록
   ```

4. **충돌 해결**: 코드 컨텍스트가 다르면 수동으로 충돌 해결

   ```bash
   # 충돌 발생 시
   git cherry-pick --continue   # 충돌 해결 후
   git cherry-pick --abort      # 취소
   ```

5. **Backport PR 생성**: 변경사항을 릴리즈 브랜치에 PR로 제출
6. **리뷰 & 테스트**: 백포트된 코드가 대상 버전 컨텍스트에서 정상 동작하는지 검증
7. **머지 & 배포**: 리뷰 통과 후 릴리즈 브랜치에 머지

### 💻 Linux Kernel의 권장 설정

```bash
# diff3 형식으로 충돌 마커 표시 (원본/현재/패치 3-way 비교)
git config --global merge.conflictstyle diff3

# 충돌 해결 후 전체 함수 컨텍스트 확인
git diff -W
```

> 📌 Linux Kernel 문서에서는 `git cherry-pick`이 `git am`보다 권장된다. 코드 이동/라인 번호 변경을 Git이 자동으로 추적할 수 있기 때문이다. ([Linux Kernel Docs](https://docs.kernel.org/process/backporting.html))

### 🤖 레이블 기반 Backport 자동화 (Label-driven Automation)

수동 cherry-pick 대신 **"PR에 레이블만 붙이면 봇이 자동으로 cherry-pick + 새 PR 생성"** 하는 워크플로를 구성할 수 있다.

```
Developer                    GitHub                     GitHub Actions
────────                    ──────                     ──────────────
   │                           │                            │
   │  1. PR에 레이블 추가       │                            │
   │  "backport release-v2.1"  │                            │
   │─────────────────────────►│                            │
   │                           │                            │
   │  2. PR 머지 (Approve)     │                            │
   │─────────────────────────►│                            │
   │                           │  3. trigger: PR closed     │
   │                           │  (merged=true)             │
   │                           │───────────────────────────►│
   │                           │                            │
   │                           │  4. 레이블 패턴 매칭        │
   │                           │  "backport release-v2.1"   │
   │                           │  → target: release-v2.1    │
   │                           │                            │
   │                           │  5. cherry-pick -x         │
   │                           │  6. 새 브랜치 생성          │
   │                           │  7. Backport PR 자동 생성   │
   │                           │◄───────────────────────────│
   │                           │                            │
   │  8. Backport PR 리뷰/머지 │                            │
   │◄─────────────────────────│                            │
```

#### 방법 1: GitHub Actions — backport-action (무료)

워크플로 파일 하나만 추가하면 바로 동작한다:

```yaml
# .github/workflows/backport.yml
name: Backport merged pull request
on:
  pull_request_target:
    types: [closed]

permissions:
  contents: write
  pull-requests: write

jobs:
  backport:
    name: Backport pull request
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged
    steps:
      - uses: actions/checkout@v4
      - uses: korthout/backport-action@v4
```

**레이블 네이밍 규칙** — 기본 패턴: `^backport ([^ ]+)$`

```
backport release-v2.1      → release-v2.1 브랜치로 백포트
backport release-v2.0      → release-v2.0 브랜치로 백포트
```

여러 레이블을 동시에 붙이면 **여러 브랜치에 동시 백포트**된다.

**커스터마이징 옵션:**

```yaml
- uses: korthout/backport-action@v4
  with:
    # 레이블 패턴 변경 (예: "bp/release-v2.1")
    label_pattern: '^bp\/([^ ]+)$'

    # PR 제목/본문 템플릿
    pull_title: '[${target_branch}] ${pull_title}'
    pull_description: |
      Backport of #${pull_number} by @${pull_author}
      Target: `${target_branch}`

    # 원본 PR 담당자를 백포트 PR에도 복사
    copy_assignees: true

    # 충돌 시 실패 대신 draft PR로 생성
    experimental: '{"conflict_resolution": "draft_commit_conflicts"}'
```

| 옵션                   | 용도                            | 기본값                                       |
| ---------------------- | ------------------------------- | -------------------------------------------- |
| `label_pattern`        | 레이블 매칭 정규식               | `^backport ([^ ]+)$`                         |
| `target_branches`      | 레이블 대신 고정 브랜치 지정      | 비어있음                                      |
| `pull_title`           | 백포트 PR 제목 템플릿            | `[Backport ${target_branch}] ${pull_title}`   |
| `copy_assignees`       | 원본 PR 담당자 복사              | `false`                                       |
| `auto_merge_enabled`   | 백포트 PR 자동 머지              | `false`                                       |

사용 가능한 템플릿 변수: `${pull_number}`, `${pull_title}`, `${target_branch}`, `${pull_author}`, `${pull_description}`, `${issue_refs}`

> 📌 출처: [korthout/backport-action](https://github.com/korthout/backport-action)

#### 방법 2: Mergify (SaaS, 풍부한 조건부 규칙)

```yaml
# .mergify.yml (레포 루트)
pull_request_rules:
  # 레이블 기반 백포트
  - name: backport to release branches
    conditions:
      - label~=^backport/
    actions:
      backport:
        branches:
          - "release-v2.1"
          - "release-v2.0"
        assignees:
          - "{{ author }}"

  # 보안 패치는 레이블 없이도 자동 백포트
  - name: auto-backport security fixes
    conditions:
      - label = security
      - merged
    actions:
      backport:
        branches:
          - "release-v2.1"
          - "release-v2.0"
          - "release-v1.9"
```

> 📌 출처: [Mergify Backport Docs](https://docs.mergify.com/workflow/actions/backport/)

#### 방법 3: GitHub CLI로 수동 레이블 관리

```bash
# 레이블 생성 (최초 1회)
gh label create "backport release-v2.1" --color 0E8A16

# PR에 레이블 부착
gh pr edit 5915 --add-label "backport release-v2.1"

# PR 생성 시 바로 레이블 지정
gh pr create --title "fix: security patch" \
  --label "backport release-v2.1" \
  --label "backport release-v2.0"
```

GitHub 웹 UI에서는 PR 오른쪽 사이드바 **Labels** 섹션에서 클릭으로 추가한다.

---

## 4️⃣ 유즈 케이스 & 베스트 프랙티스 (Use Cases & Best Practices)

### 🎯 대표 유즈 케이스

| #   | 유즈 케이스              | 설명                                         | 적합한 이유                                      |
| --- | ------------------------ | -------------------------------------------- | ------------------------------------------------ |
| 1   | **보안 취약점 패치**      | CVE 수정을 모든 운영 버전에 적용              | 보안 패치는 모든 지원 버전에 즉시 전파 필수        |
| 2   | **크리티컬 버그 핫픽스**  | 운영 장애 유발 버그를 현재 릴리즈에 적용       | 고객 영향 최소화를 위해 빠른 배포 필요             |
| 3   | **LTS 버전 유지보수**     | 장기 지원 버전에 선별적 수정 적용              | 전체 업그레이드 없이 안정성 유지                   |
| 4   | **규제 준수 패치**        | HIPAA, FDA 규정 변경에 따른 수정 반영          | 규정 위반 없이 기존 인증 버전 유지                 |

### ✅ 베스트 프랙티스

1. **Main-first 원칙 준수**: 모든 수정은 예외 없이 main에 먼저 머지. 그 후 백포트
2. **원자적(Atomic) 커밋 유지**: 백포트할 변경사항은 하나의 논리적 단위로 분리하여 커밋
3. **`-x` 플래그 사용**: cherry-pick 시 원본 커밋 해시를 추적하여 이력 관리
4. **역순으로 백포트**: v2.2 → v2.1 → v2.0 순서로 최신 릴리즈부터 적용
5. **자동화 도입**: 수동 cherry-pick 대신 Backport Bot/Action 활용으로 휴먼 에러 감소

### 🏢 실제 적용 사례

- **Linux Kernel**: `stable` 브랜치로 보안/버그 패치를 체계적으로 백포트. 전담 메인테이너가 존재 ([Kernel Docs](https://docs.kernel.org/process/backporting.html))
- **GitLab**: 자체 [Backport 가이드라인](https://handbook.gitlab.com/handbook/engineering/releases/backports/)을 운영하며, 매 릴리즈마다 이전 2개 마이너 버전에 백포트 수행
- **현재 프로젝트 (VitalCare)**: `VITALCARES-4598` 커밋이 `develop`과 `release-v2-2-1` 양쪽에 존재하는 것이 백포트의 실제 사례

---

## 5️⃣ 장점과 단점 (Pros & Cons)

| 구분    | 항목                        | 설명                                                              |
| ------- | --------------------------- | ----------------------------------------------------------------- |
| ✅ 장점 | **다중 버전 동시 유지보수**  | 고객별 다른 버전을 사용하더라도 모두 보안 패치 가능                 |
| ✅ 장점 | **선택적 변경 전파**         | 전체 업그레이드 없이 필요한 수정만 이식                             |
| ✅ 장점 | **리스크 최소화**            | 검증된(main에서 이미 테스트된) 변경사항만 적용                      |
| ✅ 장점 | **릴리즈 안정성 보장**       | 불필요한 기능 변경 없이 핵심 수정만 반영                            |
| ❌ 단점 | **Merge Conflict 위험**     | 브랜치 간 코드 분기가 클수록 충돌 가능성 증가                       |
| ❌ 단점 | **유지보수 부담 증가**       | 지원 버전이 많을수록 백포트 작업량 급증                              |
| ❌ 단점 | **미묘한 버그 유입 가능**    | 동일 커밋이라도 컨텍스트 차이로 다르게 동작할 수 있음                |
| ❌ 단점 | **커밋 히스토리 분리**       | cherry-pick은 새 커밋을 생성하므로 브랜치 간 히스토리 불일치         |

### ⚖️ Trade-off 분석

```
다중 버전 지원     ◄───── Trade-off ─────►  유지보수 비용
선택적 패치        ◄───── Trade-off ─────►  충돌 해결 노력
릴리즈 안정성      ◄───── Trade-off ─────►  히스토리 복잡도
빠른 패치 배포     ◄───── Trade-off ─────►  테스트 범위 확대
```

---

## 6️⃣ 차이점 비교 (Comparison)

### 📊 비교 매트릭스

| 비교 기준   | **Backport (Cherry-pick)**          | **Backmerge**                   | **Forward Port**                    |
| ----------- | ----------------------------------- | ------------------------------- | ----------------------------------- |
| 방향        | 새 버전 → 구 버전                    | 릴리즈 → 개발                    | 구 버전 → 새 버전                    |
| 핵심 목적   | 이전 릴리즈에 패치 적용              | 릴리즈 수정을 main에 동기화      | 이전 버전 변경을 최신에 반영          |
| Git 명령    | `git cherry-pick`                   | `git merge`                     | `git cherry-pick` 또는 `merge`       |
| 충돌 빈도   | 중~높음 (분기 정도에 따라)           | 낮음 (지연 시 높아짐)            | 낮음                                 |
| 히스토리    | 새 커밋 생성 (중복)                  | 머지 커밋 생성                   | 새 커밋 생성 (중복)                   |
| 적합한 팀   | 대규모 팀, 모노레포                  | 소규모 팀, 단일 릴리즈            | 다중 버전 프로젝트                    |

### 🔍 핵심 차이 요약

```
Backport                       Backmerge
──────────────────    vs    ──────────────────
main → release              release → main
선택적 커밋 이식              전체 브랜치 병합
커밋 단위 제어               한번에 동기화
충돌 가능성 높음              충돌 가능성 낮음
히스토리 분리                 히스토리 연결
```

### 🤔 언제 무엇을 선택?

- **Backport(Cherry-pick)를 선택하세요** → 대규모 팀, 모노레포, 여러 릴리즈를 동시 운영, 선별적 패치가 필요한 경우 ([Runway Blog](https://www.runway.team/blog/cherry-picks-vs-backmerges-whats-the-right-way-to-get-fixes-into-your-release-branch))
- **Backmerge를 선택하세요** → 소규모 팀, 릴리즈 브랜치 하나만 운영, 간단한 워크플로 선호
- **Forward Port를 선택하세요** → 패치가 이전 버전에서 먼저 발견되어 최신 버전에도 반영해야 할 때

---

## 7️⃣ 사용 시 주의점 (Pitfalls & Cautions)

### ⚠️ 흔한 실수 (Common Mistakes)

| #   | 실수                                  | 왜 문제인가                                                      | 올바른 접근                                                |
| --- | ------------------------------------- | ---------------------------------------------------------------- | ---------------------------------------------------------- |
| 1   | **Main 건너뛰고 릴리즈에 직접 수정**  | 변경사항이 main에 누락되어 다음 릴리즈에서 regression 발생        | 반드시 main에 먼저 머지 후 백포트                           |
| 2   | **충돌을 대충 해결**                   | 변수명 혼동(`i`/`j`), 에러 핸들링 누락 등 미묘한 버그 유입        | `diff3` 모드로 3-way 비교, `git diff -W`로 전체 함수 확인   |
| 3   | **테스트 없이 백포트**                 | 동일 코드라도 대상 버전의 의존성/컨텍스트가 달라 다르게 동작       | 대상 브랜치에서 반드시 빌드 + 테스트 수행                    |
| 4   | **다수 버전에 순서 없이 백포트**       | 중간 버전 누락, 일관성 깨짐                                       | 최신 → 과거 순서로 체계적 적용                               |

### 🚫 Anti-Patterns

1. **"Big Bang" 백포트**: 수십 개 커밋을 한꺼번에 cherry-pick하면 충돌 폭발. 커밋을 작게 유지하고 하나씩 적용
2. **Backport 전용 브랜치 방치**: 백포트 PR을 생성해놓고 리뷰/머지하지 않아 브랜치가 계속 분기되는 현상

### 🔒 보안/성능 고려사항

- ⚡ **에러 핸들링 경로 확인**: Linux Kernel 문서에 따르면 `goto`, `return`, `break` 등 에러 경로는 충돌 마커에 나타나지 않아 누락되기 쉬움 ([Linux Kernel Docs](https://docs.kernel.org/process/backporting.html))
- 🔒 **보안 패치의 경우 빠른 백포트 필수**: CVE 공개 후 패치 지연은 공격 표면을 노출시킴
- ⚡ **자동 포맷터(Prettier, Black 등) 주의**: 포맷 변경이 섞인 커밋은 cherry-pick 시 불필요한 충돌 유발

---

## 8️⃣ 개발자가 알아둬야 할 것들 (Developer's Toolkit)

### 📚 학습 리소스

| 유형        | 이름                              | 링크/설명                                                                                                                                          |
| ----------- | --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 📖 공식 문서 | Git Cherry-pick Docs              | [git-scm.com/docs/git-cherry-pick](https://git-scm.com/docs/git-cherry-pick)                                                                      |
| 📖 실무 가이드 | Linux Kernel Backporting Guide | [docs.kernel.org](https://docs.kernel.org/process/backporting.html)                                                                                |
| 📖 기업 사례 | GitLab Backports Handbook          | [handbook.gitlab.com](https://handbook.gitlab.com/handbook/engineering/releases/backports/)                                                         |
| 📘 블로그    | Cherry-picks vs Backmerges        | [Runway Blog](https://www.runway.team/blog/cherry-picks-vs-backmerges-whats-the-right-way-to-get-fixes-into-your-release-branch)                   |

### 🛠️ 관련 도구 & 라이브러리

| 도구/라이브러리                                                        | 플랫폼         | 용도                                     |
| ---------------------------------------------------------------------- | -------------- | ---------------------------------------- |
| [Mergify Backport](https://docs.mergify.com/workflow/actions/backport/) | GitHub/GitLab  | PR 레이블 기반 자동 백포트                |
| [git-backporting](https://github.com/kiegroup/git-backporting)         | GitHub/GitLab  | CLI + GitHub Action 백포트 자동화         |
| [backport-action](https://github.com/korthout/backport-action)         | GitHub Actions | PR 머지 시 자동 cherry-pick + PR 생성     |
| [Backport Bot](https://github.com/marketplace/actions/backport-bot)    | GitHub Actions | 레이블 기반 멀티 브랜치 백포트            |

### 🔮 트렌드 & 전망

- **자동화 우선**: 수동 cherry-pick에서 CI/CD 통합 자동 백포트로 전환 추세
- **레이블 기반 워크플로**: PR에 `backport/v2.x` 레이블만 붙이면 봇이 자동으로 백포트 PR 생성
- **모노레포 최적화**: 대규모 모노레포에서의 선택적 백포트 도구 발전

### 💬 커뮤니티 인사이트

- Cherry-pick 방식의 백포트는 **대규모 팀과 모노레포에서 사실상 필수**이며, 소규모 팀에서는 오버헤드가 클 수 있다는 것이 커뮤니티의 일반적 견해
- dbt 프로젝트는 ["The case against git cherry-pick"](https://docs.getdbt.com/blog/the-case-against-git-cherry-picking)이라는 글에서 cherry-pick이 오히려 리스크를 높인다고 주장하며 대안적 브랜치 전략을 제시
- **실무 팁**: 커밋 메시지에 원본 PR 번호와 Jira 티켓을 함께 기록하면 추적성이 크게 향상됨

---

## 📎 Sources

1. [Linux Kernel Backporting and Conflict Resolution](https://docs.kernel.org/process/backporting.html) — 공식 문서
2. [Cherry-picks vs Backmerges — Runway Blog](https://www.runway.team/blog/cherry-picks-vs-backmerges-whats-the-right-way-to-get-fixes-into-your-release-branch) — 기술 블로그
3. [GitLab Backports Handbook](https://handbook.gitlab.com/handbook/engineering/releases/backports/) — 기업 가이드
4. [Git Cherry-pick Official Docs](https://git-scm.com/docs/git-cherry-pick) — 공식 문서
5. [git-backporting (kiegroup)](https://github.com/kiegroup/git-backporting) — 오픈소스 도구
6. [Mergify Backport Action](https://docs.mergify.com/workflow/actions/backport/) — 자동화 도구
7. [backport-action (korthout)](https://github.com/korthout/backport-action) — GitHub Action
8. [Bareos Git Workflow](https://docs.bareos.org/DeveloperGuide/gitworkflow.html) — 실무 가이드

---

> 🔬 **Research Metadata**
> - 검색 쿼리 수: 6
> - 수집 출처 수: 8
> - 출처 유형: 공식 2, 블로그 3, 도구/오픈소스 3
